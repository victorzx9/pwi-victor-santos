// Solicita ao usuário os valores de A, B e C
let A = 12
let B = 14
let C = 30

// Calcula a soma entre A e B
let soma = A + B;

// Exibe a soma entre A e B
console.log(`A soma entre A e B é: ${soma}`);

// Verifica se a soma é menor que C e exibe o resultado
if (soma < C) {
  console.log("A soma é menor que C.");
}
